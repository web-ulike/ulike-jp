 {%- liquid
  assign product = section.settings.product
-%}
<section>
 {% for variant in product.variants %}
  <div id="{{ variant.id }}"> {{ variant.option1  }} - {{ variant.price | money }} - 
     <img src="{{ variant.featured_image.src | image_url }}"/ >
   <img src="{{  variant.featured_media.preview_image.src | image_url }}"/ >
  </div>

{% endfor %}
</section>
<script>
</script>
{% schema %}
{
  "name": "Test Product",
  "settings": [
    {
      "type": "product",
      "id": "product",
      "label": "Product"
    }
  ],

  "presets": [
    {
      "name": "Test Product"
    }
  ]
}
{% endschema %}
